// ===== Google Maps config =====
// Paste your Google Maps API key here (Google Cloud Console → APIs & Services →
// Credentials). IMPORTANT: restrict the key by HTTP referrer to your site AND set a
// daily quota cap, so a leaked key can never run up charges (it is public in the page).
//
// Leave empty to keep using the free OSM / Esri layers only.
// Street View works even without a key.
window.GMAPS_CONFIG = {
    key: ''    // e.g. 'AIzaSy...'
};
