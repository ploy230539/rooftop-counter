// ===== Cloud config (Supabase) =====
// Fill these in AFTER creating your free Supabase project (Project Settings → API).
// Both values are safe to be public — the anon key is protected by Row Level Security
// and by the fact that public sign-ups are disabled (only your 2 accounts exist).
//
// Leave both empty to keep using local (in-browser) storage.
window.CLOUD_CONFIG = {
    url: '',       // e.g. 'https://abcdefgh.supabase.co'
    anonKey: ''    // the "anon public" key (a long eyJ... string)
};
